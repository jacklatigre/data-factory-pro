apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-factory-ingress
  namespace: data-factory-app
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: data-factory.192.168.64.100.nip.io # Aggiornato a .100
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: data-factory-service
            port:
              number: 80
